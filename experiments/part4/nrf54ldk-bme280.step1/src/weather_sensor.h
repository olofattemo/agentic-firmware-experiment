#ifndef WEATHER_SENSOR_H
#define WEATHER_SENSOR_H

/**
 * @brief Thread entry point for the sensor acquisition task.
 * Implements: LLR-WX-002
 */
void weather_sensor_task_entry(void *p1, void *p2, void *p3);

#endif // WEATHER_SENSOR_H
